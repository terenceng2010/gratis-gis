chore(web-map): diagnostic logging for icon rendering 
 
Temporary console.log in syncOverlays emits icon state when a 
layer is in icon mode: which image ids are checked, whether 
they're registered, how many icons total have loaded. Makes it 
possible to distinguish a registration failure from a symbol-
layer paint issue without running a full React debugger.
